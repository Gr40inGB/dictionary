spring:
  application:
    name: dictionary
  datasource:
    url: jdbc:postgresql://localhost:5432/dictionary
    username: richard
    password: 5565
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
        format_sql: true

server:
  port: 9090

